{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "$apis": ["src/apis"],
      "$apis/*": ["src/apis/*"],
      "$common": ["src/common"],
      "$common/*": ["src/common/*"],
      "$jwt": ["src/jwt"],
      "$jwt/*": ["src/jwt/*"],
      "$middlewares": ["src/middlewares"],
      "$routes": ["src/routes"],
      "$routes/*": ["src/routes/*"],
      "$types": ["src/types"],
      "$types/*": ["src/types/*"]
    }
  },
  "include": ["src"],
  "exclude": ["node_modules"]
}
